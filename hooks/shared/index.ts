/**
 * @fileoverview Shared Hooks - Index
 * @description Central export point for all shared utility hooks
 */

// Authentication and user management
export { useAuth } from './use-auth';

// Storage and state persistence
export { useLocalStorage } from './use-local-storage';

// Performance and optimization
export { useDebounce, useDebouncedValue } from './use-debounce';

// UI and interaction utilities
export { useIntersectionObserver } from './use-intersection-observer';
